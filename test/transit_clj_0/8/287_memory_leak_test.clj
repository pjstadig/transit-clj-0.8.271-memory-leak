(ns transit-clj-0.8.287-memory-leak-test
  (:require [clojure.test :refer :all]
            [transit-clj-0.8.287-memory-leak :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
